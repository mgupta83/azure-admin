{
  "name": "azure-admin",
  "version": "1.0.0",
  "description": "Azure Monitoring UI - Monorepo for Azure infrastructure monitoring",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "type-check": "turbo run type-check"
  },
  "devDependencies": {
    "turbo": "^1.13.4",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "npm@10.2.4",
  "engines": {
    "node": ">=18.0.0"
  }
}